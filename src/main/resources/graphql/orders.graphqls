extend type Query {
    orderById(id: ID!): Order
}

type Order {
    id: ID!,
    creationDate: DateTime
    orderDetails: [OrderDetail]!
}

type OrderDetail {
    id: ID!
    quantity: Int!
    book: Book!
    price: Float!
}